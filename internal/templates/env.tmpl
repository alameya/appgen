# Application settings
APP_NAME=app

# Database settings
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=postgres

# Server settings
PORT=8080

# Migrations settings
MIGRATIONS_DIR=./migrations

# JWT settings
JWT_SECRET=secret 